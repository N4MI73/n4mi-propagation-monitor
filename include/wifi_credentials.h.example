// N4MI Desktop Instrument Series - Propagation Monitor
// wifi_credentials.h.example -- TEMPLATE
//
// Copy this file to wifi_credentials.h (same folder) and fill in your
// real network name/password. wifi_credentials.h itself is listed in
// .gitignore and must never be committed -- this .example file is the
// only version that goes into the public repo.
//
// This is temporary scaffolding for proving out the real PropMon HTTP
// fetch before the real captive-portal Wi-Fi setup (Phase 3+) exists.
// Once that's built, credentials move to NVS (entered once via the
// on-device setup flow) and this file goes away entirely.

#pragma once

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"
